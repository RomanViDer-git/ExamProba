<Window x:Class="Obuv_Prob.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Obuv_Prob"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="Обувной магазин" FontSize="32" Grid.Column="1" Width="376" HorizontalAlignment="Left"/>
        <TextBlock Text="{Binding CurrentUser.User_Name}"  HorizontalAlignment="Right" Margin=" 0 0 10 0" FontSize="12" Grid.Column="1" Grid.Row="0"/>
        <Image Grid.Column="0" Source="/pictures/Icon.png"/>
        <TabControl Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch">
            <TabItem Header="Главная">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBox Padding="5" BorderBrush="Gray" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" Width="300" FontSize="15" KeyDown="SearchTB_KeyDown" Name="SearchTB"/>
                        <StackPanel VerticalAlignment="Center" Margin="5">
                            <RadioButton x:Name="SortRB" Content="А-Я" Checked="SortRB_Checked" VerticalAlignment="Center"/>
                            <RadioButton x:Name="SortDesRB" Content="Я-А" Checked="SortDesRB_Checked" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Производитель" Margin="5 0 5 1"/>
                            <ComboBox Name="ManufaturerCB" Width="100" Margin="5 0 5 1" SelectionChanged="ManufaturerCB_SelectionChanged"/>
                        </StackPanel>
                    </StackPanel>
                    <ListBox Name="ProductLB" Grid.Row="1" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="DarkGray" Padding="5" Margin="5" BorderThickness="3" CornerRadius="15">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>
                                        <Border BorderBrush="DarkGray" Padding="5" Margin="5" BorderThickness="1" CornerRadius="15">
                                            <StackPanel Grid.Column="0" >
                                                <Image Source="{Binding Photo}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <Border BorderBrush="DarkGray" Padding="5" Margin="5" BorderThickness="1" Grid.Column="1" CornerRadius="15">
                                            <StackPanel >
                                                <StackPanel Orientation="Horizontal" Margin="10 5 15  0">
                                                    <TextBlock Text="{Binding Category.Category_Name}" FontWeight="Bold" FontSize="12"/>
                                                    <TextBlock Text=" | " FontWeight="Bold" FontSize="12"/>
                                                    <TextBlock Text="{Binding Product_Name}" FontWeight="Bold" FontSize="12"/>
                                                </StackPanel>
                                                <TextBlock Text="{Binding Discription, StringFormat='Описание товара: {0}'}" FontSize="10" Margin="10 1 15  0"/>
                                                <TextBlock Text="{Binding Manufactures.Manufacturer_Name, StringFormat='Производитель: {0}'}" FontSize="10" Margin="10 1 15  0"/>
                                                <TextBlock Text="{Binding Suppliers.Supplier_Name, StringFormat='Поставщик: {0}'}" FontSize="10" Margin="10 1 15  0"/>
                                                <TextBlock Text="{Binding Price, StringFormat='Цена: {0}'}" FontSize="10" Margin="10 1 15  0"/>
                                                <TextBlock Text="{Binding Unit, StringFormat='Единаца измерения: {0}'}" FontSize="10" Margin="10 1 15  0"/>
                                                <TextBlock Text="{Binding Quantity, StringFormat='Количество на складе: {0}'}" FontSize="10" Margin="10 1 15  0"/>
                                            </StackPanel>
                                        </Border>
                                        <Border BorderBrush="DarkGray" Padding="5" Margin="5" BorderThickness="1" Grid.Column="2" CornerRadius="15">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center"  HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding Discount, StringFormat='{}{0}%'}" Background="LightGreen" FontSize="15"/>
                                            </StackPanel>
                                        </Border>
                                        
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
            <TabItem Header="Админ">
                <DataGrid Name="ProductDG" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Артикуль" Binding="{Binding Article}"/>
                        <DataGridTextColumn Header="Дата заказа" Binding="{Binding Order_Date, StringFormat={}{0:dd.MM.yyyy}}" />
                        <DataGridTextColumn Header="Дата доставки" Binding="{Binding Delivery_Date, StringFormat={}{0:dd.MM.yyyy}}"/>
                        <DataGridTextColumn Header="Адрес" Binding="{Binding Pu_Point.Address}"/>
                        <DataGridTextColumn Header="Код получения" Binding="{Binding Code}"/>
                        <DataGridTextColumn Header="Получатель" Binding="{Binding Users.User_Name}"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding StatusOrder.Status}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
